<?php
/*
|--------------------------------------------------------------------------
| BOOKS
|--------------------------------------------------------------------------
*/

if ($type === 'book') {

    $meta = get_field('author');

    $cover = get_field('cover_image');

    $image = $cover
        ? $cover['sizes']['medium']
        : '';
}
